#[tokio::main]
async fn main() -> anyhow::Result<()> {
    // make sure flag is enabled,
    // make sure make is installed,
    // make sure seaorm is installed,
    // run generate script with make,

    Ok(())
}

// sea-orm-cli generate entity
// --database-url postgresql://usr:pwd@localhost:5432/db
// --database-url public
// --date-time-crate time
// --with-serde both
// --verbose
